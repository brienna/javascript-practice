// 3.1. Minimum

// Write a function min that takes two arguments and returns their minimum.

function min(x, y) {
  if (x < y) 
    return x;
  else if (x > y)
    return y;
}


// Eloquent JavaScript answer:
function min(a, b) {
  if (a < b)
    return a;
  else
    return b;
}
